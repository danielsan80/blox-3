from config import Config

# def mv(point, pos=[0,0,0], step = mv_step ):
#     [point.x+pos.x*step, point.y+pos.y*step, point.z+pos.z*step];
#
# module mv(pos=[0,0,0], step = play+wall_w/2 ) {
#     translate([pos.x*step, pos.y*step, pos.z*step])
#     children();
# }
