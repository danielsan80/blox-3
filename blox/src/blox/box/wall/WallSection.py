class WallSection:
    def __init__(self, dir, length, start, offset, turn):
        self.dir = dir
        self.length = length
        self.start = start
        self.offset = offset
        self.turn = turn